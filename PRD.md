# MenuTemp PRD — macOS 菜单栏 CPU 温度实时显示

## 1. 背景

在 Apple Silicon（M1）Mac 上，以**最小资源开销**在菜单栏实时显示 CPU 温度。

## 2. 目标

- 菜单栏**直接显示温度数字**，如 `48°`，实时刷新（≤2s）
- 免 root、免装额外驱动，读写 SMC 传感器
- 空闲开销尽可能小：CPU ≈ 0%、内存 < 40MB、几乎不影响电量
- 下拉菜单可查看全部传感器温度并退出

## 3. 非目标（明确不做）

- 不做历史曲线/图表
- 不监控内存、磁盘、网络等其他指标
- 不做复杂配置界面
- 不支持 Intel Mac（如后续需要可降级为 `osx-cpu-temp` 读取）

## 4. 技术方案

| 项 | 选择 | 说明 |
|---|---|---|
| 系统要求 | macOS 13+ | 使用原生 `MenuBarExtra`（macOS 13 引入） |
| 芯片 | Apple Silicon 专用 | M1 实测；其他 M 系列待验证 |
| 温度来源 | HID SMC 事件接口 | `AppleARMPMUTempSensor` 节点，免 root |
| CPU 温度定义 | `max(PMU tdie1..8)` | M1 无独立 CPU 传感器，tdie 即芯片封装温度 |
| 架构 | C helper 常驻 + SwiftUI App | helper 事件驱动推送，App 只管 UI |

### 架构图

```
SwiftUI MenuBarExtra App（菜单栏 "48°"）
        │  NSPipe 读取 stdout，按行解析
        ▼
C helper (smctemp)  常驻进程
  ├─ HID 事件回调（推送式，非轮询）
  ├─ 按传感器名缓存最新温度
  └─ 值更新即输出 "name value" 行
```

- App 启动时拉起 helper，helper 异常退出则自动重启
- 温度读取为**事件驱动**，空闲时 helper 几乎零 CPU

## 5. 交互与视觉

- 菜单栏：`48°`（当前 CPU 温度，取最大 tdie）；`>85°` 时文字变红
- 下拉菜单：
  - 全部传感器名称 + 当前温度（左对齐名称，右对齐数值）
  - 「退出」
- 无 Dock 图标（`LSUIElement`）

## 6. 验收指标

| 指标 | 目标 |
|---|---|
| 菜单栏刷新延迟 | ≤ 2s |
| 空闲 CPU 占用 | < 0.5% |
| 常驻内存 | < 40MB |
| 读数合理性 | 20–110°C 范围内，与系统传感器趋势一致 |
| 无 root / 无额外安装 | 是 |

## 7. 测试策略

1. **helper 单元测试**：反复运行，断言输出格式合法、数值在合理区间、无崩溃
2. **集成测试**：App 启动后能收到 helper 数据，CPU 温度被正确计算
3. **手动验收**：菜单栏文字显示、下拉列表、颜色告警、退出

## 8. 里程碑

1. 验证 HID 事件接口可读温度（关键风险，先做）
2. helper 稳定输出，跑通测试
3. App 在菜单栏正确显示温度
4. 打包 `.app`、支持登录启动、推送 GitHub

## 9. 风险与对策

| 风险 | 对策 |
|---|---|
| HID 事件客户端类型/API 随 macOS 版本变化 | 尽量用公开稳定的调用；helper 单文件便于快速 patch |
| 传感器命名因机型而异（tdie 缺失等） | 取 tdie 最大值，缺失时回退到任一可用温度传感器 |
| 传感器在部分机型不可读 | 菜单栏显示 `--`，下拉菜单标注不可用 |
| Intel 支持 | 明确不支持（PRD 范围），后续可用 `osx-cpu-temp` 分支 |
